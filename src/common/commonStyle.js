import { StyleSheet } from 'react-native'

export const commonStyle = StyleSheet.create({
    container: {
        flex: 1
    },
    row: {
        flexDirection: 'row'
    },
    column: {
        flexDirection: 'column'
    }
})